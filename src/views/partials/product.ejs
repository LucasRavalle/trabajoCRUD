<% products.map(product => { %>
    <div class="col-12 col-sm-6 col-lg-3">
    <section class="product-box">
        <a href="/products/detail/<%= product.id %> ">
            <figure class="product-box_image">
                <img src="/images/products/<%= product.image %> " alt="imagen de producto">
            </figure>
            <article class="product-box_data">
                <h2> $ <%= toThousand(product.price) %> </h2>

               <!--  Con este condicional se busca NO mostrar el "descuento" de 0% en la vista, ya que
                esto no sumaría a la experiencia de usuario. En caso de que haya descuento, que 
                se muestre. Caso contrario, nada. -->

                <% if (product.discount !== 0) { %> 
                    <span><%= product.discount %> % OFF</span>
                <% } %>      
                          
                <p><%= product.name %></p>
                <i class="fas fa-truck"></i>
            </article>
        </a>
    </section>
</div>
<% }) %>